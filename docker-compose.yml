version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./tesla_tokens.json:/app/tesla_tokens.json
      - ./tesla_orders.json:/app/tesla_orders.json
    restart: unless-stopped
